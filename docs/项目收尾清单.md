# 项目收尾清单

> 目标：把当前版本从“已经能跑、功能很多”收口到“结构清晰、协议稳、功能可验证、可交付发布”的状态。

## 一、完成标准

## 当前已完成的结构收尾（截至本轮）
- [x] 聊天页已拆分为 `ChatScreen.kt` / `ChatScreenSupport.kt` / `ChatConversationPane.kt` / `ChatScreenOverlays.kt` / `ChatScreenBehavior.kt` / `ChatScreenChrome.kt` / `ChatScreenLocalState.kt`
- [x] Provider 详情页已拆分为 `ProviderDetailScreen.kt` / `ProviderConfigTab.kt` / `ProviderModelTab.kt` / `ProviderModelDialogs.kt` / `ProviderDetailChrome.kt`
- [x] 聊天气泡已拆分为 `MessageBubble.kt` / `MessageBubbleState.kt` / `MessageBubbleLayout.kt` / `MessageBubblePresentation.kt` / `MessageBubbleAttachments.kt` / `MessageBubbleActions.kt`
- [x] 沉浸设置页已拆分为 `RoleplaySettingsScreen.kt` / `RoleplaySettingsContent.kt`
- [x] 沉浸主页面已拆分为 `RoleplayScreen.kt` / `RoleplayScreenContent.kt`
- [x] 沉浸对话面板已拆分为 `RoleplayDialoguePanel.kt` / `RoleplayDialoguePanelParts.kt`
- [x] 沉浸式 ViewModel 已拆分为 `RoleplayViewModel.kt` / `RoleplayObservationSupport.kt` / `RoleplayRoundTripExecutor.kt` / `RoleplayScenarioActionSupport.kt` / `RoleplaySuggestionActionSupport.kt` / `RoleplayContextUpdateSupport.kt` / `RoleplaySendActionSupport.kt`

### 工程门禁
- [x] `testDebugUnitTest` 全绿
- [x] `lintDebug` 全绿
- [x] `assembleDebug` 全绿
- [ ] 工作区 clean，无大面积未整理改动

### 功能门禁
- [ ] 聊天主链路人工回归通过
- [ ] 沉浸式主链路人工回归通过
- [ ] Provider 配置链路人工回归通过
- [ ] OpenAI Compatible / Anthropic 协议都至少实测一轮
- [x] 沉浸设置切模型后的“实际发送模型生效”已完成真机闭环验证

### 发布门禁
- [ ] 版本号、更新 JSON、CHANGELOG 收口
- [ ] 最终 APK 安装验证通过

---

## 二、P0：必须优先完成

### 1. Provider 接口抽象继续收口
当前已经支持：
- OpenAI Compatible `/chat/completions`
- Anthropic `/v1/messages`

当前已完成的第一阶段：
- [x] OpenAI Compatible 下可选择 `Responses API` 模式（配置层与主要调用链已接入）
- [x] 自定义 Provider 已可配置 `chatCompletionsPath`
- [x] 已参考 RikkaHub 的 `useResponseApi` / `chatCompletionsPath` 设计，开始把“协议 + 路径 + 能力”拆成更严谨的 Provider 配置层
- [x] `AiGateway` 已支持 OpenAI Compatible 下的 `Chat Completions / Responses API` 两种文本模式
- [x] `AiTranslationService` 已支持 OpenAI Compatible 下的 `Chat Completions / Responses API` 两种文本模式
- [x] `AiPromptExtrasService` 已接入 `Responses API` 第一阶段支持（标题/摘要/建议/记忆链路可按模式走）
- [x] `Responses API` 与自定义 `chatCompletionsPath` 的单测已补首轮覆盖

当前还未完成但已明确要做：
- [ ] Responses API 兼容性做实测与回归覆盖（尤其第三方/自定义端点）
  - [x] 已补 `AiGateway` / `AiTranslationService` / `AiPromptExtrasService` 的 Responses API 单测首轮覆盖
  - [x] 已补自定义 `chatCompletionsPath` 的首轮单测覆盖
- [ ] 把“协议 + 路径 + 能力”继续抽象成更完整的 Provider 能力模型

但还不够严谨，后续必须补齐：
- [ ] 为自定义（第三方）Provider 增加更明确的 API 模式抽象
  - [ ] Chat Completions
  - [ ] Responses API
  - [ ] Anthropic Messages
- [x] 允许自定义 Provider 明确声明是否走 `/responses`
- [ ] 不要仅按品牌/ProviderType 推断接口能力，要允许“第三方 OpenAI 风格端点支持 Responses API”的情况
- [ ] 对自定义 Provider 增加端点级能力配置：
  - [x] `chat/completions`
  - [x] `responses`
  - [ ] `images/generations`
  - [ ] `models`
- [ ] 健康检查不要只判“能不能 list models”，要能区分：
  - [ ] 鉴权错误
  - [ ] 路径不支持
  - [ ] 协议不匹配
  - [ ] 端点存在但字段不兼容
- [ ] 在 Provider 配置页增加更清晰的说明文案：
  - [ ] 当前协议走什么路径
  - [ ] 当前模式适用于什么类型的网关/中转站
  - [ ] 如果失败，优先排查哪个配置项

#### 参考实现
- [ ] 参考 RikkaHub 的 Provider 配置思路
  - 本地截图参考：
    - `D:\lucky0401\data\weixin\xwechat_files\wxid_zthu4ln765yu22_39d6\temp\RWTemp\2026-03\9e20f478899dc29eb19741386f9343c8\82fdacf7c7c4edd4af4b5ac0110463f0.jpg`
  - 代码参考：
    - `rikkahub_ref/ai/src/main/java/me/rerere/ai/provider/ProviderSetting.kt`
    - `rikkahub_ref/ai/src/main/java/me/rerere/ai/provider/providers/OpenAIProvider.kt`
- [x] 重点参考其 `useResponseApi` / `chatCompletionsPath` 设计
- [ ] 结合本项目现状设计成“可扩展但不过度复杂”的 Provider 协议层

### 2. 聊天 / 沉浸式核心功能回归
- [x] 已补《人工回归测试清单》：`docs/人工回归测试清单.md`
- [x] 已完成一轮真机非侵入式冒烟导航验证：聊天页 / 设置页 / 翻译页 / 沉浸扮演页可打开
- [x] 已完成第二轮真机 UI 深入验证：Provider 列表/详情/协议切换/文本模式切换/模型页、沉浸会话/沉浸设置/模型选择入口可达
- [x] 已完成沉浸式“对白模式”排版与对白高亮修复，并已在真机复验通过
- [x] 已修复对白模式里修辞性内嵌引号误拆导致的“上文不接下文”问题
- [x] 已支持沉浸式进入会话时默认定位到最新回复
- [x] 已为聊天页 / 沉浸式输入框增加展开编辑入口，改善长输入浏览与编辑体验
- [x] 已修复 RP 协议输出里“未标记 narration 的叙述段被误判成对白”的问题，并完成真机复验
- [ ] 已补第二轮 RP 协议兼容修复（`<dialogue>` 内混写动作/旁白），待下次设备连线后补真机复验
- [ ] 已补沉浸式消息区回顶 / 回底快捷按钮，已完成首轮真机验证（静止隐藏 / 上滑回顶），待继续补回底按钮手感复验
- [x] 已为“当前角色已改绑”弹窗增加“不再显示”选项
- [x] 已补首轮《人工回归执行记录》：`docs/人工回归执行记录-2026-03-28-首轮.md`
- [ ] 文本消息发送/流式展示
- [ ] 图片附件发送
- [ ] 文件附件发送
- [ ] 生图模型模式
- [ ] thinking budget 生效
- [ ] 会话切换 / 删除 / 清空
- [ ] 聊天翻译 / 草稿翻译 / 翻译结果回填
- [ ] 转账玩法链路
- [ ] 沉浸式：进入、继续、重开、清空、编辑回退、建议生成、长文模式、阅读模式
- [x] 沉浸设置页切模型确认为“视觉上切换 + 实际发送模型也切换”（已真机实测 `glm-5 -> deepseek-chat -> deepseek-v3.1`）

### 3. APK 与更新闭环
- [x] 确认当前 dev 包安装正常（已做真机冒烟验证）
- [x] 更新 `CHANGELOG.md`
- [x] 已新增《发布候选记录》并记录当前 dev 包 SHA256：`docs/发布候选记录.md`
- [ ] 如需内置更新测试，确认 `docs/updates/dev.json`
- [x] 已完成一轮基础真机冒烟测试：`docs/真机冒烟验证记录-2026-03-28.md`
- [ ] 最终发包前做一次冷启动 + 主流程冒烟测试

---

## 三、P1：强烈建议完成

### 4. 继续做结构收尾，不再无边界拆分
目标不是继续“无限拆文件”，而是把高复杂区域压到合理范围。

#### 建议目标
- [ ] `ChatScreen.kt` 继续收口到 500 行左右（当前约 626 行）
- [x] `RoleplayViewModel.kt` 继续收口到 500 行左右（当前约 436 行）
- [ ] 检查新拆出的 support 文件职责边界是否清晰
- [ ] 避免重复逻辑在 support 间扩散

### 5. Provider 页体验加强
- [x] Provider 配置页增加协议切换后的即时提示
- [x] Provider 配置页增加 OpenAI 文本接口模式切换（Chat Completions / Responses API）
- [x] Provider 配置页增加 `chatCompletionsPath` 自定义入口
- [x] 已做真机 UI 验证：协议切换、文本模式切换、`Chat Completions Path` 显隐联动正常
- [ ] 选择模型后增加“下次发送立即生效”提示
- [ ] 配置错误时提示更聚焦
- [ ] 自定义 Provider 文案从“品牌”转向“协议能力”

### 6. 图片与富媒体链路复核
- [ ] 生图模型时禁用附件是否在所有入口一致
- [ ] 视觉模型与非视觉模型的图片发送限制是否一致
- [ ] Anthropic 协议下图片输入能力是否明确
- [ ] 第三方兼容端点下图片生成/图片理解能力是否配置清晰

---

## 四、P2：收尾增强项

### 7. 测试补强
- [ ] 补关键 Compose / AndroidTest 冒烟测试
- [x] Responses API 相关单测已补首轮覆盖
- [x] 已补《人工回归执行记录模板》：`docs/人工回归执行记录模板.md`
- [x] 已补《缺陷回填模板》：`docs/缺陷回填模板.md`
- [ ] 至少覆盖：
  - [ ] 聊天主页发送
  - [ ] Provider 配置
  - [ ] 沉浸设置切模型
  - [ ] 角色扮演基本对话

### 8. 文案与空状态统一
- [ ] 统一错误提示文案
- [ ] 统一成功提示文案
- [ ] 统一空状态和引导文案
- [x] 聊天页与沉浸式输入区已统一提供“展开编辑”入口

### 9. 真机兼容验证
- [ ] Pixel / AOSP
- [x] 小米（首轮非侵入式导航冒烟已验证）
- [ ] OPPO / OnePlus
- [ ] vivo
- [ ] 三星

重点验证：
- [ ] 悬浮翻译
- [ ] 无障碍
- [ ] MediaProjection
- [ ] APK 安装更新

---

## 五、当前建议执行顺序
1. [ ] 继续完善 Provider 协议抽象（尤其 Responses API）
2. [ ] 重点回归聊天与沉浸式主链路
3. [ ] 做版本与更新 JSON 收口
4. [ ] 做最终 APK 冒烟验证
5. [ ] 再做剩余结构微调与测试补强
