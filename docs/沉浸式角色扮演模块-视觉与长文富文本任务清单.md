# 沉浸式角色扮演模块 - 视觉与长文富文本任务清单

- 对应计划：`docs/沉浸式角色扮演模块-视觉与长文富文本改造计划.md`
- 创建日期：2026-03-24
- 当前负责人：AI 协作执行
- 当前状态：执行中（核心功能已落地，继续补交互收尾）

## 一、当前迭代目标

先完成以下高优先级内容：

1. 建立长文内部渲染标记协议。
2. 建立安全的长文富文本解析与展示链路。
3. 避免标签在 UI、复制、流式输出中泄漏。
4. 改造沉浸式动态配色，去掉固定冷蓝感。
5. 提升阅读模式的正文阅读体验。
6. 补齐对话页身份条、状态条、情绪胶囊与用户消息重回编辑能力。

## 二、任务拆分

### A. 文档与跟踪

- [x] 新建改造计划文档
- [x] 新建任务跟踪文档
- [x] 在 `进度和日志进展.md` 记录本轮改造进展

### B. 长文内部渲染协议

- [x] 修改 `RoleplayPromptDecorator.kt`
- [x] 为长文模式追加 `<char>` / `<thought>` 内部协议说明
- [x] 保持正式输出禁止 Markdown / 代码块 / 额外说明

### C. 富文本解析基础设施

- [x] 新增 `RoleplayLongformMarkupParser.kt`
- [x] 支持 `<char>` 标签解析
- [x] 支持 `<thought>` 标签解析
- [x] 支持段落拆分
- [x] 支持剥离标签得到正式文本
- [x] 支持未闭合标签 / 异常标签兜底

### D. UI 数据模型与映射

- [x] 修改 `RoleplayMessageUiModel.kt`
- [x] 为长文消息补充 `richTextSource`
- [x] 修改 `RoleplayViewModel.kt` 长文消息映射逻辑
- [x] 长文消息 `content` 改为无标签纯文本
- [x] 长文消息 `copyText` 改为无标签纯文本
- [x] 长文消息 `richTextSource` 保留原始语义标记

### E. 对话页与长文渲染

- [x] 修改 `RoleplayLongformText.kt`
- [x] 长文卡片接入富文本解析
- [x] 角色台词使用强调样式
- [x] 心声使用弱强调样式
- [x] 无标签时正常回退为纯文本显示
- [x] 修改 `RoleplayDialoguePanel.kt` 接入长文富文本源
- [x] 优化对话页颜色语义

### F. 动态配色

- [x] 修改 `ImmersiveGlass.kt`
- [x] 保留背景主色相，不再强制冷蓝化
- [x] 派生角色强调色 / 用户强调色 / 心声色 / 阅读容器色
- [x] 将对话页主要颜色切换到动态语义色

### G. 阅读模式重构

- [x] 修改 `RoleplayReadingMode.kt`
- [x] 增加阅读容器
- [x] 提升字号 / 行高 / 段落间距
- [x] 降低背景对正文的干扰
- [x] 让用户消息在阅读页中降权显示

### H. 身份条 / 状态条 / 情绪胶囊

- [x] 在 `RoleplayScreen.kt` 接回顶部身份条
- [x] 在 `RoleplayScreen.kt` 接回状态条胶囊
- [x] 新增 `RoleplayEmotionChip.kt`
- [x] 对话页 emotion 改为胶囊显示
- [x] 阅读页 emotion 改为胶囊显示

### I. 用户消息重回与编辑

- [x] 在 `RoleplayViewModel.kt` 新增 `editUserMessage()`
- [x] 用户长按消息支持“重回并编辑”
- [x] 重回时回滚该用户消息及其后续剧情
- [x] 恢复输入框内容，允许直接修改后重发
- [x] 重回并编辑后自动聚焦输入框

### J. 失败回合视觉提示

- [x] 失败回合增加显眼提示胶囊
- [x] 失败的角色对白增加弱错误背景
- [x] 失败的长文消息增加弱错误底色与标题强调

### K. 验证与测试

- [x] 新增 `RoleplayLongformMarkupParserTest.kt`
- [x] 补充 `RoleplayViewModelTest.kt` 长文富文本映射断言
- [x] 补充 `RoleplayViewModelTest.kt` 用户消息重回编辑断言
- [x] 运行相关单元测试
- [x] 至少完成一次 `compileDebugKotlin` 或等效编译验证

## 三、执行日志

## 2026-03-24

- [x] 已确认产品方向：对话页沉浸优先，长文页阅读优先。
- [x] 已确认富文本策略：只高亮角色本人说的话。
- [x] 已确认允许模型输出内部标签，但正式展示内容不得出现标签。
- [x] 已新建计划文档与任务清单。
- [x] 已完成长文内部标记协议接入：`<char>` / `<thought>`。
- [x] 已完成长文富文本解析器与 UI Model 安全映射。
- [x] 已完成对话页动态语义色替换，去掉固定冷蓝强调色。
- [x] 已完成阅读模式阅读容器与长文排版增强。
- [x] 已完成顶部身份条 / 状态条回接。
- [x] 已完成 emotion 胶囊显示。
- [x] 已完成用户消息“重回并编辑”能力。
- [x] 已完成重回并编辑后的输入框自动聚焦。
- [x] 已完成失败回合的显眼提示与弱错误背景。
- [x] 已补充单元测试并通过 `testDebugUnitTest` 定向验证。
