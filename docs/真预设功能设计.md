# 真预设（Preset）功能设计文档

> 文档定位：长期规划的设计稿，非当前迭代任务。
> 目标读者：未来动手实现这套预设系统的人。
> 关联：[`tavo_alignment_plan.md`](../tavo_alignment_plan.md) 的 P2 项；[`docs/tavo-alignment-completed-2026-04-25.md`](tavo-alignment-completed-2026-04-25.md) 的"预设"语义复盘。

---

## 2026-04-29 二版实施记录

`1.6.3-dev` 已把真预设从“数据地基”推进到可运行的 Prompt Manager：

1. `Preset` 保留旧 `systemPrompt` / `contextTemplate` 兼容字段，并新增 `entries: List<PresetPromptEntry>` 与 `renderConfig`，Room `presets` 表通过 JSON 字段承载条目和渲染配置，不新增大表。
2. 内置 Narra 默认预设变为只读 Prompt Manager，条目包含 Main Prompt、角色描述、角色提示词、用户身份、场景、示例对话、摘要、世界书、长记忆、手机线索、Chat History 和状态栏规则。
3. `PresetPromptRenderer` 按条目顺序渲染真实请求脚手架：`Chat History` 前的 `SYSTEM` 条目合并进系统提示词，非 `SYSTEM` 前置条目进入历史前消息，`Chat History` 后的条目进入后置消息。
4. `PromptContextAssembler` 现在会解析角色默认预设、全局默认预设与内置 fallback，并把渲染结果通过 `PromptEnvelope` 传给普通聊天和沉浸聊天主回复。
5. `AiGateway` 接收 `PromptEnvelope`，支持预设后置消息、采样参数和停止词；OpenAI Chat Completions、Responses 和 Anthropic 请求体都补齐了对应字段映射。
6. 设置页新增“预设管理”入口，列表页支持查看、搜索、复制、设默认、删除自定义预设；编辑页支持条目搜索、添加、复制、开关、角色切换、上下移动、内容编辑和最终顺序预览。
7. 内置预设不可被 UI 或导入逻辑直接覆盖；导入或复制出来的预设会落为 `builtIn=false` 的自定义预设。
8. 助手详情页新增“默认预设”选择入口；`AppSettings.defaultPresetId` 作为全局默认，新角色会继承全局默认。
9. 新增安全状态卡：识别 `<StatusBlock>`、`<status>`、`<user_status>`、`status/statusbar/状态栏` 代码块与状态栏段落，渲染为可折叠 Compose 卡片，不执行 HTML/JS。
10. 状态栏识别继续扩展到 `>『时间 / 地点 / 状态』` 这类社区常见多行状态栏；正文里的 `<p style="text-align:center; color:gray; font-size:0.9em">`、`<content>`、`<br>` 等安全模板标签会走 Compose 富文本白名单渲染，避免原样露出前端代码。

当前仍未做：SillyTavern 预设 JSON 完整兼容、Instruct Mode prefix/suffix 的运行时包装、会话级预设覆盖、资源市场、HTML 状态栏执行。

安全边界：Narra 不用 WebView 执行模型输出，也不执行 `<script>`、CSS 动画、事件处理器或任意 HTML/JS。图片来源仍沿用现有 Markdown / `<img src="">` 提取链路渲染为图片附件；HTML 只解析为文本、分隔线、对齐、颜色、字号等安全表现。

---

## 一、为什么要做"真预设"

### 当前命名混乱

仓库里"预设"一词被三种不同语义共用：

| 出现位置 | 实际含义 | 距离 SillyTavern Preset |
|----------|---------|------------------------|
| ~~`ContextLogSourceType.PRESET`~~（已重命名为 `ROLE_EXTRAS`） | 角色卡边角字段（scenario / greeting / example_dialogues） | ❌ 完全不同 |
| ~~`RoleplaySettingsSidebarPanel` 的"预设"卡片~~（已重命名为"人设提示词"） | `Assistant.systemPrompt` 字段 | ❌ 完全不同 |
| `PRESET_ASSISTANT_ICONS` / `ReasoningBudgetPreset` 等 | 与 SillyTavern Preset 无关，仅仅是同名词 | — |

P0b 已经把前两处错位的命名修掉了。但**项目里至今没有一个真正的 SillyTavern 风格 Preset 概念**——这是 P2 要补上的能力。

### SillyTavern Preset 真正包含什么

参考酒馆社区文档与现存预设包格式（如 `Magnum-V2`、`MarinaraSpaghetti` 系列），一个完整 Preset 应当是**与角色无关、与提供商无关**的"提示词工程模板包"，至少包含：

1. **System Prompt 主模板**：包装角色描述、人设、记忆、世界书的 master prompt（含占位符 `{{char}}` / `{{user}}` / `{{description}}` 等）
2. **Context Template / Story String**：决定"角色描述 + 角色人设 + 场景 + 示例对话 + 历史"如何拼接的最外层模板
3. **Instruct Mode 设置**：
   - User / Assistant / System 各自的 prefix / suffix（如 `<|im_start|>user\n` / `<|im_end|>\n`）
   - Wrap with newlines、Skip Special Tokens、System Same as User 等开关
   - Stop Sequences（停止字符串列表）
4. **Sampler 参数**：Temperature / Top P / Top K / Min P / Repetition Penalty / Frequency Penalty / Presence Penalty / Mirostat 等
5. **附加注入项**：Author's Note、Memory Position、Last Assistant Prefix、Continue Prompt 等（可选）

我们项目里这些能力目前**全部缺失或散落**：sampler 在 `ProviderSettings` / `Assistant.modelOverrides` 里硬编码、上下文模板在 `PromptContextAssembler` 里 buildString 拼出、Instruct 标签完全没有抽象、Stop Sequences 不存在。

---

## 二、目标范围

### 必做（MVP）
- [x] 引入 `Preset` 数据实体（独立于 Assistant、ProviderSettings 的第三类配置）
- [x] `Preset` 持久化到 Room（多预设可切换）
- [x] System Prompt 主模板 + 条目列表占位符渲染，接入 `PromptContextAssembler`
- [x] Sampler 参数从 Preset 取值并透传到主聊天请求
- [x] 设置主页新增"预设"入口，提供列表 + 编辑 + 应用切换
- [x] 内置 1～2 个开箱可用预设（Narra 默认预设、思考模型稳态）
- [x] 当前角色的预设选择（每个 Assistant 可绑定默认预设）

### 可选（V1 之后）
- [ ] Instruct Mode 抽象（user/assistant/system prefix-suffix），目前主流后端用 OpenAI Chat Completion 不强需要
- [ ] Author's Note 注入位置控制
- [ ] 预设导入导出（JSON 格式，兼容 SillyTavern Preset JSON）
- [ ] 预设市场/分享

### 明确不做
- 不复刻 SillyTavern 的 ST 扩展机制（那是 JS 沙箱方案）
- 不做正则替换 / regex script（独立功能，已有占位卡片，未来另立专题）
- 不做 quick reply 按钮（聊天 UI 范畴，独立专题）

---

## 三、数据模型设计

### 3.1 核心实体

```kotlin
// 概念草图，真实实现时根据 Room 约束细化
data class Preset(
    val id: String,                        // UUID
    val name: String,                      // 显示名
    val description: String = "",          // 简介
    val systemPrompt: String,              // master prompt 模板（含占位符）
    val contextTemplate: String,           // story string 模板（含占位符）
    val sampler: PresetSamplerConfig,      // 采样参数
    val instructMode: PresetInstructConfig? = null,  // 可选 Instruct
    val stopSequences: List<String> = emptyList(),
    val createdAt: Long,
    val updatedAt: Long,
    val builtIn: Boolean = false,          // 内置预设标记
)

data class PresetSamplerConfig(
    val temperature: Float? = null,
    val topP: Float? = null,
    val topK: Int? = null,
    val minP: Float? = null,
    val repetitionPenalty: Float? = null,
    val frequencyPenalty: Float? = null,
    val presencePenalty: Float? = null,
    val maxOutputTokens: Int? = null,
    // null 表示"不覆盖"，沿用 ProviderSettings 默认
)

data class PresetInstructConfig(
    val systemPrefix: String,
    val systemSuffix: String,
    val userPrefix: String,
    val userSuffix: String,
    val assistantPrefix: String,
    val assistantSuffix: String,
    val wrapWithNewlines: Boolean = true,
)
```

### 3.2 占位符规范

`systemPrompt` / `contextTemplate` 中可使用的占位符（与 SillyTavern 兼容）：

| 占位符 | 替换内容 | 来源 |
|--------|---------|------|
| `{{char}}` | 角色名 | `Assistant.name` |
| `{{user}}` | 用户名 | `AppSettings.userPersona.name` |
| `{{description}}` | 角色描述 | `Assistant.systemPrompt` 主体 |
| `{{personality}}` | 性格摘要 | `Assistant.personality`（可能要新增字段） |
| `{{scenario}}` | 场景设定 | `Assistant.scenario` |
| `{{example_dialogue}}` | 示例对话 | `Assistant.exampleDialogues` |
| `{{world_info}}` | 命中的世界书条目 | `WorldBookRepository.listAccessibleEnabledEntries(...)` |
| `{{long_memory}}` | 长记忆条目 | `MemoryRepository.observeEntries()` |
| `{{summary}}` | 剧情摘要 | `ConversationSummary` |
| `{{date}}` `{{time}}` | 当前日期时间 | 系统 |

### 3.3 持久化

- 新建 Room 实体 `PresetEntity` + DAO `PresetDao`
- `ChatDatabase` 版本 +1（当前 v27 → v28），迁移在 `ChatDatabase.kt` 里写一条 `Migration(27, 28)` `CREATE TABLE`
- 新建 Repository `PresetRepository`（复用 `data/repository/context/` 目录组织）
- 注入到 `AppGraph`：`val presetRepository by lazy { ... }`
- 内置预设走"启动时检查并 upsert"，标记 `builtIn = true`，禁止删除

### 3.4 与 Assistant 的关联

`Assistant` 加一个字段 `defaultPresetId: String? = null`，迁移时所有现存 Assistant 默认指向"中文角色扮演通用"内置预设。

会话级别也可临时覆盖：`Conversation` 加 `overridePresetId: String? = null`（可选，初版可跳过）。

---

## 四、注入点改造

### 4.1 `PromptContextAssembler` 重构

当前 `assemble()` 方法里硬编码了：
- `【角色描述】\n` `【场景设定】\n` `【开场白参考】\n` `【示例对话】\n` 等小标题
- 各段拼接顺序

改造后：
- `Preset.systemPrompt` 替换硬编码主体
- `Preset.contextTemplate` 替换硬编码段落拼接
- `ContextPlaceholderResolver` 扩展：除了现有的 `{{char}}` `{{user}}`，新增 `{{description}}` `{{world_info}}` 等的解析

向后兼容：当 `Preset` 不存在时（用户卸载默认预设），fallback 到当前的硬编码逻辑。但 MVP 期间通过"内置预设永远存在"的契约规避这种情况。

### 4.2 `AiGateway` / Sampler 透传

当前 sampler 参数在 `ApiServiceFactory.createService` → `OpenAiCompatibleApi.completion` 路径里从 `ProviderSettings` 取。改造后：
- `AiGateway.sendChat(...)` 增加 `preset: Preset?` 参数（可空，为空时维持当前行为）
- 取值优先级：`Preset.sampler.* != null` ？ `Preset.sampler.*` : `Assistant.modelOverrides.*` : `ProviderSettings.*`
- 新增字段（如 `min_p`、`repetition_penalty`）需检查 `OpenAiCompatibleApi` 请求体是否支持；不支持的 provider 静默丢弃，UI 上对该字段做"该提供商不支持"灰显

### 4.3 Stop Sequences 接入

`stopSequences` 在请求体里追加 `"stop": [...]`，OpenAI / Anthropic / Gemini 都支持但字段名略有差异，已有 `ProviderApiProtocol` 枚举处理协议差异，新增 sampler 字段沿用同一抽象。

---

## 五、UI 草图（文字版）

### 5.1 设置主页新增入口

`SettingsScreen.kt` 的"模型与服务"分组追加：

```
[ ⚙️ 默认模型和提示词 ]
[ 🧠 提供商         ]
[ 🎨 预设           ] ← 新增（在提供商之后）
[ 🔍 搜索与工具     ]
```

或归到"通用设置"分组，紧贴"助手"，按"哪一组的语义更偏内容生成模板"决定（暂建议进"模型与服务"）。

### 5.2 预设列表页

```
┌─────────────────────────────────┐
│ ←   预设管理         [+ 添加]   │
├─────────────────────────────────┤
│ ◉ 中文角色扮演通用     内置  ⋮  │
│   master prompt + 标准 sampler  │
├─────────────────────────────────┤
│ ○ 思考模式优化         内置  ⋮  │
│   适合 reasoning model 的低温度  │
├─────────────────────────────────┤
│ ○ 我的自定义预设               ⋮ │
│   2026-04-20 创建                │
└─────────────────────────────────┘
```

- 单选指示符 `◉` 表示当前全局默认预设
- 右侧 `⋮`：编辑 / 复制 / 删除（内置不可删）/ 导出
- 底部 + 添加 = 弹出"复制现有 / 创建空白"二选一

### 5.3 预设编辑页

分段标签栏（横向 tab）：

```
[ 基本 ] [ System Prompt ] [ Context ] [ Sampler ] [ 高级 ]
```

- **基本**：名称、描述、是否设为默认
- **System Prompt**：大文本区，monospace，下方占位符快速插入按钮
- **Context**：Context Template 大文本区 + 占位符按钮
- **Sampler**：Temperature / Top P / Top K / Min P / Repetition Penalty 等滑块或数值输入；每项右侧"恢复默认"按钮（清空回退到 Provider 默认）
- **高级**：Stop Sequences 列表编辑、Instruct Mode 折叠区（默认收起）

### 5.4 助手详情页绑定

`AssistantBasicScreen.kt` 加一个"默认预设"选择行，下拉式 BottomSheet。

---

## 六、迁移路径与拆分

### 阶段 1：数据模型 + 内置预设（已完成，1.6.3-dev）
- [x] 新增 `Preset` 实体、`PresetDao`、`PresetRepository`、Room 迁移
- [x] `AppGraph` 注入
- [x] 启动时 upsert 2 个内置预设（"中文角色扮演通用"、"思考模型稳态"）
- [x] `Assistant.defaultPresetId` 新字段
- [x] 接入资料库预设包导入导出
- [x] **不改任何调用方**，运行时行为完全不变

### 阶段 2：占位符引擎扩展 + Context Template 接管（独立 PR）
- `ContextPlaceholderResolver` 增加新占位符
- `PromptContextAssembler` 内部新增 `assembleWithPreset(preset: Preset)` 私有方法，与现有逻辑并行
- 通过功能开关或仅在某个调试入口启用，对照测试输出一致性
- 单测覆盖占位符渲染各种边界

### 阶段 3：Sampler 透传 + UI 列表/编辑页（独立 PR）
- `AiGateway` 接受 `preset` 参数
- 设置主页 + 预设列表 + 预设编辑页
- 助手详情页加预设选择
- 真机回归：聊天 / 角色扮演 / 长记忆提取 三条流程都要确认无回归

### 阶段 4：默认预设切换为新引擎（独立 PR，颠覆式）
- 移除 `PromptContextAssembler` 中硬编码字符串
- 全部走 Preset 引擎
- 旧 `Assistant.systemPrompt` 字段降级为内部用：仍然是"角色描述"主体，只是不再直接当 master prompt
- 真机全量回归

### 阶段 5：进阶能力（按需）
- 导入 SillyTavern Preset JSON
- Instruct Mode 完整实现
- 会话级覆盖

---

## 七、风险与开放问题

### 7.1 OpenAI Chat Completion 与 Instruct Mode 冲突
当前所有 provider 都走 Chat Completion API（messages 数组），Instruct Mode 是 raw text completion 的概念。MVP 阶段建议跳过 Instruct，只做 Chat 路径下能用的 sampler + system prompt + context template；除非接入 llama.cpp / Ollama 这类 raw 接口才补 Instruct。

### 7.2 占位符渲染与世界书懒加载冲突
当前 `WorldBookRepository.listAccessibleEnabledEntries` 是 suspend 函数。如果 `{{world_info}}` 占位符渲染发生在 `PromptContextAssembler.assemble()`（已经 suspend 上下文）内部，没问题；如果未来允许在编辑页"预览渲染结果"，需要保证调用方在协程里。

### 7.3 多 sampler 字段的 provider 兼容
- OpenAI 标准支持 temperature / top_p / frequency_penalty / presence_penalty
- Anthropic 支持 temperature / top_p / top_k
- Gemini 支持 temperature / topP / topK / max_output_tokens
- llama.cpp / Ollama 支持几乎全部
- 建议在 `ProviderSettings` 上增加 `supportedSamplers: Set<SamplerKind>` 元数据，UI 编辑页根据当前默认 provider 给字段灰显提示

### 7.4 内置预设升级策略
内置预设在新版本可能要升级（修 bug 或调优默认 sampler）。策略：
- 每个内置预设带 `version` 字段
- 启动时如果发现内置预设的 `version` 高于本地，**仅当用户没自定义修改过它**才覆盖
- 用户改过的版本标记 `userModified = true`，永不覆盖；但通过 UI 提供"恢复出厂"按钮

### 7.5 与 RAG/工具调用的边界
`{{world_info}}` 当前是关键词触发的硬注入，未来可能换成 RAG。Preset 的 `contextTemplate` 应该把"世界书命中"作为一个独立可选段，而非和角色描述焊死在一起，方便未来替换。

### 7.6 测试策略
- `PresetRepository` 用 in-memory Room（已有 `ChatDatabaseMigrationRegistryTest` 模板）
- 占位符渲染：纯逻辑单测，黄金样本对比
- `PromptContextAssembler` 整合测试：给定 fake Preset + fake Assistant，断言 assembled prompt 字符串
- 真机回归：必做，所有"对模型说话"的入口都跑一遍

---

## 八、工程量粗估

| 阶段 | 估时 | 说明 |
|------|------|------|
| 阶段 1 | 1～2 天 | Room schema + Repository + 内置预设种子 |
| 阶段 2 | 2～3 天 | 占位符引擎 + 双轨对照 + 单测 |
| 阶段 3 | 4～5 天 | UI 三屏（列表/编辑/绑定）+ Sampler 透传 + 真机回归 |
| 阶段 4 | 1～2 天 | 切默认引擎 + 全量回归 |
| 阶段 5 | 2～3 天/项 | 进阶能力按需追加 |

**总计 MVP（阶段 1～4）：约 8～12 工作日**。

---

## 九、决策记录

- ❌ 不复刻 SillyTavern 全套预设规范（含 Authors Note、Memory Position 等）：MVP 范围太大
- ✅ 占位符语法对齐 SillyTavern：方便未来导入第三方预设
- ✅ Preset 与 Assistant 解耦（Preset 是独立实体）：避免预设字段污染 Assistant
- ✅ 多预设并存：默认全局预设 + 助手默认预设 + 会话覆盖（V1）三层
- ⚠️ 阶段 4 是颠覆式破坏：完成后 `Assistant.systemPrompt` 不再直接当 master prompt，需要文档/迁移说明同步

---

## 十、下一步行动

本文档先冻结。等 P1（API 连接合并）完成、`tavo_alignment_plan.md` 清空后，按本设计的"阶段 1"启动单独 PR，把数据模型层落地，作为整套预设系统的地基。

> 实施时务必先在新分支验证 Room migration 27 → 28 的 schema 一致性（`ChatDatabaseMigrationRegistryTest` 框架已就位）。
